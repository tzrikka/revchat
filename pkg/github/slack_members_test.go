package github

/*
import (
	"testing"
)

func TestWithArticle(t *testing.T) {
	tests := []struct {
		name string
		want string
	}{
		{
			name: "assignee",
			want: "an assignee",
		},
		{
			name: "reviewer",
			want: "a reviewer",
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := withArticle(tt.name); got != tt.want {
				t.Errorf("withArticle() = %q, want %q", got, tt.want)
			}
		})
	}
}
*/
